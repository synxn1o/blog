## File features

1. change the upload method to direct .md upload
2. add math render support

## Layouts

1. support for no cover image (cover.jpg)
2. add Chinese font support (that has similar effects as English fonts)
3. change the backgroud of index page to rolling images (rolling in selected photos, instead of the fixed one)
4. fix blur effect of nav bar on index page (that should be the same as /blog page, with blur effect)

## Pages

1. add a photowall page (uses markdown file as container, auto multi columns layout, hover to see remarks when avaliable)
2. add links page
3. replace the "featured" part on index page with a link to the photowall, same layout, display first photo of photowall, but with fewer text on the right "See my Photographics >"
4. remove the "latest" part on index page, replace it with /blog page's list layout, keep the category filter on top right corner.
